deployment:
  production:
    branch: master
    commands:
      - git config --global user.name "hubukinokaze"
      - git config --global user.email "yudai.j.kawa@gmail.com"
      - sed -i'' "s~https://github.com/hubukinokaze/hubukinokaze.github.io.git~https://adbb449e29813bbc15db28fcce083430d5b78437:x-oauth-basic@github.com/hubukinokaze/hubukinokaze.github.io.git~" _config.yml
      - rm -rf .deploy_git/
      - hexo clean
      - hexo generate
      - hexo deploy